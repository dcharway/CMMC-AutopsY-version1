import { Box, Typography } from '@mui/material';

export function ControlRegistry() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Control Registry</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        Component loading...
      </Typography>
    </Box>
  );
}
