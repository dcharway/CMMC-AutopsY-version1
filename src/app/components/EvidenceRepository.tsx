import { Box, Typography } from '@mui/material';

export function EvidenceRepository() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Evidence Repository</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        Component loading...
      </Typography>
    </Box>
  );
}
